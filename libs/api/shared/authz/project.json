{
  "name": "api-shared-authz",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/api/shared/authz/src",
  "projectType": "library",
  "tags": ["scope:api", "type:util", "domain:shared"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{workspaceRoot}",
        "commands": [
          "tsc -p libs/api/shared/authz/tsconfig.lib.json --noEmit",
          "tsc -p libs/api/shared/authz/tsconfig.spec.json --noEmit"
        ],
        "parallel": false
      }
    }
  }
}
