{
  "name": "api-shared-auth",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/api/shared/auth/src",
  "projectType": "library",
  "tags": ["scope:api", "type:feature", "domain:shared"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{workspaceRoot}",
        "commands": [
          "tsc -p libs/api/shared/auth/tsconfig.lib.json --noEmit",
          "tsc -p libs/api/shared/auth/tsconfig.spec.json --noEmit"
        ],
        "parallel": false
      }
    }
  }
}
