{
  "name": "api-polls-application",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/api/polls/application/src",
  "projectType": "library",
  "tags": ["scope:api", "type:application", "domain:polls"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{workspaceRoot}",
        "commands": [
          "tsc -p libs/api/polls/application/tsconfig.lib.json --noEmit",
          "tsc -p libs/api/polls/application/tsconfig.spec.json --noEmit"
        ],
        "parallel": false
      }
    }
  }
}
