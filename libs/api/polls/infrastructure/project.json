{
  "name": "api-polls-infrastructure",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/api/polls/infrastructure/src",
  "projectType": "library",
  "tags": ["scope:api", "type:infrastructure", "domain:polls"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{workspaceRoot}",
        "commands": [
          "tsc -p libs/api/polls/infrastructure/tsconfig.lib.json --noEmit",
          "tsc -p libs/api/polls/infrastructure/tsconfig.spec.json --noEmit"
        ],
        "parallel": false
      }
    },
    "test-integration": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "libs/api/polls/infrastructure",
        "command": "vitest run --config vitest.integration.config.mts"
      }
    }
  }
}
