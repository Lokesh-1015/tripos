{
  "name": "api-trips-application",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/api/trips/application/src",
  "projectType": "library",
  "tags": ["scope:api", "type:application", "domain:trips"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{workspaceRoot}",
        "commands": [
          "tsc -p libs/api/trips/application/tsconfig.lib.json --noEmit",
          "tsc -p libs/api/trips/application/tsconfig.spec.json --noEmit"
        ],
        "parallel": false
      }
    }
  }
}
