{
  "name": "api-trips-domain",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/api/trips/domain/src",
  "projectType": "library",
  "tags": ["scope:api", "type:domain", "domain:trips"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{workspaceRoot}",
        "commands": [
          "tsc -p libs/api/trips/domain/tsconfig.lib.json --noEmit",
          "tsc -p libs/api/trips/domain/tsconfig.spec.json --noEmit"
        ],
        "parallel": false
      }
    }
  }
}
