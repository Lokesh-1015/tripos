/**
 * Shared design system.
 *
 * Today this library owns the design tokens at `src/styles/tokens.css`, imported
 * by `apps/web/src/app/global.css`. Reusable presentational components (shadcn
 * primitives, then TripOS-specific composites) land here in M1 — props in, markup
 * out, with no data fetching (CLAUDE.md §13).
 */
export {};
