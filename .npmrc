# Fail fast if the local Node/pnpm does not satisfy package.json "engines".
engine-strict=true

# Surface peer dependency problems instead of silently installing a broken tree.
# This is how ADR-0009's version conflicts would have shown up at install time.
strict-peer-dependencies=false
auto-install-peers=true
