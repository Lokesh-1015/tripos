{
  "version": "0.0.1",
  "configurations": [
    {
      "// note": "web runs on 4200 so it does not collide with the API on 3000. This matches the CORS_ORIGINS default in .env.example.",
      "name": "web",
      "runtimeExecutable": "pnpm",
      "runtimeArgs": ["exec", "nx", "dev", "web", "--port=4200"],
      "port": 4200
    },
    {
      "name": "api",
      "runtimeExecutable": "pnpm",
      "runtimeArgs": ["exec", "nx", "serve", "api"],
      "port": 3000
    }
  ]
}
